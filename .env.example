############################################################
# Example environment configuration (copy to .env locally) #
############################################################
SECRET_KEY=change_me
DEBUG=False

# Domains
ALLOWED_HOSTS=*.cloudtype.app,uniroom.shop,www.uniroom.shop,localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=https://*.cloudtype.app,https://uniroom.shop,https://www.uniroom.shop,http://localhost:3000,http://127.0.0.1:3000

# CORS
CORS_ALLOW_ALL=False
CORS_ALLOWED_ORIGINS=https://*.cloudtype.app,https://uniroom.shop,https://www.uniroom.shop,http://localhost:3000,http://127.0.0.1:3000

# Security (enable in production via Cloudtype dashboard)
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True

# Django / DRF extras (uncomment if added in settings later)
# ACCESS_TOKEN_LIFETIME_MINUTES=60
# REFRESH_TOKEN_LIFETIME_DAYS=7
